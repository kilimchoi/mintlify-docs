---
title: 'v1/remove-background'
api: 'POST https://randomseed.lol/v1/remove-background'
description: 'Remove background image'
---
### API key

Create your account on [RandomSeed](https://randomseed.co/signup) & grab your [api key](https://randomseed.co/api-reference/integrate)

### Headers

<ParamField header="Authorization" type="string">
  Your API key. This is required by most endpoints to access our API programatically. You can get your api key by clicking on 'API reference' tab under your profile icon.
</ParamField>

### Body

<ParamField body="image_url" type="string" required>
  Link to the image
</ParamField>

<ParamField body="webhook_url" type="string">
  Callback url that will be called.
</ParamField>

### Response

<ResponseField name="image_url" type="string">
  Image url will be returned
</ResponseField>

<RequestExample>

```bash Example Request
curl --location "https://randomseed.lol/v1/remove-background" \
--header "Content-Type: application/json" \
--header "Authorization: Bearer $RANDOMSEED_API_KEY" \
--data '{
    "remove_bg": {
        "image_url": "..." //replace with your own
    }
}
'
```
</RequestExample>

<ResponseExample>

```json Response
{
    "image_url": "..."
}
```
</ResponseExample>
